name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build, test and lint
    runs-on: macos-15
    env:
      DERIVED_DATA: .build/DerivedData

    steps:
      - uses: actions/checkout@v4

      # Pick the newest Xcode on the image: its SDK is always >= every installed
      # simulator runtime, so each runtime stays usable. Pinning an older Xcode
      # fails when the image only ships newer runtimes.
      - name: Select Xcode
        run: |
          XCODE=$(ls -d /Applications/Xcode*.app | sort -V | tail -1)
          echo "Using ${XCODE}"
          sudo xcode-select -s "${XCODE}"
          xcodebuild -version
          xcrun simctl list runtimes

      - name: Validate package manifest
        run: swift package dump-package --package-path PasswordCryptor > /dev/null

      # Resolve a concrete iPhone simulator by UDID. Names can be ambiguous
      # across runtimes, so match on the device id instead.
      - name: Resolve simulator
        id: simulator
        run: |
          UDID=$(xcrun simctl list devices available --json | python3 -c '
          import json, sys
          devices = json.load(sys.stdin)["devices"]
          for entries in devices.values():
              for entry in entries:
                  if entry.get("isAvailable") and entry["name"].startswith("iPhone"):
                      print(entry["udid"])
                      sys.exit(0)
          sys.exit(1)
          ')
          if [ -z "${UDID}" ]; then
            echo "error: no available iPhone simulator found." >&2
            exit 1
          fi
          echo "udid=${UDID}" >> "$GITHUB_OUTPUT"

      - name: Run package tests
        run: swift test --package-path PasswordCryptor

      - name: Check formatting
        run: swift format lint --recursive --strict PasswordCryptor/Sources PasswordX PasswordXAuth Shared

      - name: Generate Xcode project
        run: |
          brew install xcodegen
          xcodegen generate

      - name: Build app
        run: |
          xcodebuild -project PasswordX.xcodeproj \
            -scheme PasswordX \
            -destination 'platform=iOS Simulator,id=${{ steps.simulator.outputs.udid }}' \
            -derivedDataPath "$DERIVED_DATA" \
            build

      - name: Build credential provider extension
        run: |
          xcodebuild -project PasswordX.xcodeproj \
            -scheme PasswordXAuth \
            -destination 'platform=iOS Simulator,id=${{ steps.simulator.outputs.udid }}' \
            -derivedDataPath "$DERIVED_DATA" \
            build
